apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: connect-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: topology-aware-standard
  resources:
    requests:
      storage: 500Mi
